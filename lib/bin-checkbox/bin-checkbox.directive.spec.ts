import { BinCheckboxDirective } from './bin-checkbox.directive';

describe('BinCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BinCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
